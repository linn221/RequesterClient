add a full width search bar on request listing page, doing the filtering on the client side, by searching the Text field, you must support regex, and display the matched results on each filtered request. For example, Matched results section should be like "hello, Coffee. lorem,kksd", "lsdsdjk dskfjksd coffee fksld". Also, bold the search text on that section.
Add the same search function to Endpoint listing page. It also have Text field.
